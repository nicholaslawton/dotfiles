configuration {
  font: "Sans-Serif Regular 12";

  drun {
    display-name: "";
  }

  run {
    display-name: "";
  }

  window {
    display-name: "";
  }

  timeout {
    delay: 10;
    action: "kb-cancel";
  }
}

* {
  clear: rgba(0, 0, 0, 0);
  frosted: rgba(0, 0, 0, 0.4);
  tint-light: rgba(255, 255, 255, 0.2);
  tint-medium: rgba(255, 255, 255, 0.4);
  tint-heavy: rgba(255, 255, 255, 0.9);
}

window {
  transparency: "real";
  background-color: @frosted;
  border: 1px;
  border-color: @tint-medium;
  border-radius: 10px;
}

mainbox {
  children: [inputbar, listview];
  padding: 10px;
  spacing: 10px;
}

inputbar {
  background-color: @tint-light;
  text-color: @tint-heavy;
  children: [prompt, entry];
}

entry {
  text-color: @tint-heavy;
  placeholder-color: @tint-medium;
  padding: 12px 3px;
}

prompt {
  text-color: @tint-heavy;
  padding: 12px;
}

listview {
  columns: 5;
  lines: 3;
  spacing: 10px;
  border: 0px;
  scrollbar: false;
}

element {
  orientation: vertical;
  children: [element-icon, element-text];
  padding: 10px;
}

element.normal.normal {
  background-color: @clear;
  text-color: @tint-heavy;
}

element.alternate.normal {
  background-color: @clear;
  text-color: @tint-heavy;
}

element.selected.normal {
  background-color: @tint-light;
  text-color: @tint-heavy;
}

element-icon {
  size: 64px;
}

element-text {
  horizontal-align: 0.5;
}
